import React from 'react';
import styles from './current-promotion.module.css'

const CurrentPromotion = ({promoText, promoLink}) => {
    return (
        <div>

        </div>
    );
};

export default MyComponent;
